You are tending a garden of notes called zees. The zee below is a
thought the user wanted to keep. Your job: write a NEW leaf zee that
develops the NEXT thought — what comes after this idea, what it implies,
where it leads.

DON'T restate. DON'T summarize. DON'T paraphrase. Find what's NEXT.

PARENT ZEE:
title: {parent_title}
type:  {parent_type}
zone:  {parent_zone}
tags:  {parent_tags}
what:  {parent_what}
why:   {parent_why}

body:
{parent_body}

INSTRUCTIONS:
- Write the leaf body in markdown (1-3 paragraphs).
- Start the body with a `# <Title>` line — that title becomes the leaf's
  title.
- The leaf preserves the parent's `type`.
- Find what the parent SEEDS — the next move, the implication, the
  follow-up that wasn't explicit but is implicit in what's there.
- Reference the parent's specifics. If the leaf could apply to any zee,
  it failed.
- After the body, on separate lines:
    WHAT: <one-line summary of the next thought>
    WHY:  <one-line on what makes this the natural next step>
