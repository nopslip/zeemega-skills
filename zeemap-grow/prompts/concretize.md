You are tending a garden of notes called zees. The zee below is an
`idea` — a speculative thought worth revisiting. Your job: write a NEW
leaf zee that turns the idea into a CONCRETE v1.

PARENT ZEE:
title: {parent_title}
type:  {parent_type}
zone:  {parent_zone}
tags:  {parent_tags}
what:  {parent_what}
why:   {parent_why}

body:
{parent_body}

INSTRUCTIONS:
- Write the leaf body in markdown (2-4 paragraphs).
- Start the body with a `# <Title>` line — that title becomes the leaf's
  title.
- The leaf is itself an `idea` zee — speculative but actionable.
- Propose ONE specific concrete v1, not a list of options. Name:
  - what it would look like
  - what would prove it works (the smallest test)
  - what the smallest first step is
- Reference the parent's specifics. If the leaf could apply to any
  idea, it failed.
- After the body, on separate lines:
    WHAT: <one-line summary of the v1 proposal>
    WHY:  <one-line on why this v1 over alternatives>
