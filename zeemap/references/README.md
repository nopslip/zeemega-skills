# References

Drop reference zees here — user-profile notes, topical anchors, or any
markdown content the skill should have durable access to alongside
captured zees. Each instance accumulates its own; nothing ships with the
skill itself.

Format: same YAML frontmatter + markdown body as zees.
