# zeemap-fetch

Read-side companion to the `zeemap` skill. Searches a user's zees by
free-text query, returns up to 5 ranked candidates as JSON.

Install path: `$HERMES_HOME/skills/productivity/zeemap-fetch/`.

See SKILL.md for trigger phrases, output shape, and pitfalls.
